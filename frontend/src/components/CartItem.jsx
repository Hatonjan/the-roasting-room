/**
 * CartItem.jsx
 * 
 * Individual cart item component
 * Displays product info, quantity selector, and remove button
 * Used in CartPage
 */

export default function CartItem({ item }) {
  return (
    <div className="cart-item">
      <h4>Cart Item</h4>
      <p>Cart item details will go here</p>
    </div>
  );
}
